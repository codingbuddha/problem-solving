package helper;

public class Strings {

    	public static void swap(char[] word, int i, int j){
    	    char temp;
    	    temp = word[i];
    	    word[i] = word[j];
    	    word[j] = temp;
    	}
}
